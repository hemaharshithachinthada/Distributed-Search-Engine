# Distributed-Search-Engine
This project is a Distributed Search Engine built using Java and Spring Boot. It stores documents across multiple servers (shards) and uses TF-IDF to rank search results. The system provides fast and reliable searching by processing queries across the distributed nodes.
